# Анализ безопасности кода (Semgrep Analysis)

## Обзор
Проведен анализ безопасности кода проекта `utip-main`, особенно модулей, связанных с работой с обращениями (tickets).

## Найденные проблемы

### 🔴 Критические проблемы

#### 1. **XSS уязвимость через dangerouslySetInnerHTML**
**Файл:** `pages/TicketsPage.tsx:259`
```tsx
dangerouslySetInnerHTML={{ __html: ticket.ticketMessage }}
```
**Проблема:** Использование `dangerouslySetInnerHTML` без санитизации может привести к XSS атакам, если `ticket.ticketMessage` содержит вредоносный HTML/JavaScript.

**Рекомендация:** 
- Использовать библиотеку для санитизации HTML (например, DOMPurify)
- Или отображать текст как plain text с экранированием

#### 2. **Хардкод API ключа в коде**
**Файл:** `entities/ticket/api/ticketApi.ts:35`
```typescript
const API_KEY = process.env.VITE_API_KEY || 'Fiugkjyu76fhjt7hbk';
```
**Проблема:** API ключ захардкожен в коде как fallback значение. Это означает, что ключ будет виден в клиентском коде.

**Рекомендация:**
- Убрать fallback значение
- Использовать только переменные окружения
- Убедиться, что ключ не попадает в production build

### 🟡 Предупреждения

#### 3. **Небезопасный JSON.parse без обработки ошибок**
**Файл:** `entities/ticket/api/ticketApi.ts:120`
```typescript
const tickets = JSON.parse(response.values || '[]');
```
**Проблема:** `JSON.parse` может выбросить исключение при невалидном JSON, что приведет к падению приложения.

**Рекомендация:**
```typescript
try {
  const tickets = JSON.parse(response.values || '[]');
} catch (e) {
  console.error('Failed to parse tickets JSON:', e);
  return [];
}
```

#### 4. **Использование alert() в production коде**
**Файл:** `pages/TicketsPage.tsx:50`
```typescript
alert(`Просмотр обращения #${ticketId}. Функция будет реализована позже.`);
```
**Проблема:** `alert()` не подходит для production кода, плохой UX.

**Рекомендация:** Заменить на модальное окно или toast-уведомление.

#### 5. **console.log в production коде**
**Файл:** `pages/TicketsPage.tsx:49`
```typescript
console.log('Show ticket:', ticketId);
```
**Проблема:** `console.log` может раскрывать чувствительную информацию в production.

**Рекомендация:** Использовать условное логирование или удалить в production build.

#### 6. **Обрезка токена до 32 символов**
**Файл:** `entities/ticket/api/ticketApi.ts:22`
```typescript
const trimmedToken = authToken.length > 32 ? authToken.substring(0, 32) : authToken;
```
**Проблема:** Обрезка токена может привести к коллизиям и проблемам с безопасностью, если несколько токенов начинаются одинаково.

**Рекомендация:** Убедиться, что это требование API и что токены уникальны в первых 32 символах.

#### 7. **Отсутствие валидации входных данных**
**Файл:** `entities/ticket/api/ticketApi.ts:128`
```typescript
export async function getTicket(
  authToken: string,
  userId: string,
  ticketId: number
)
```
**Проблема:** Нет проверки, что `ticketId` является положительным числом.

**Рекомендация:** Добавить валидацию входных параметров.

#### 8. **Отсутствие обработки ошибок сети**
**Файл:** `entities/ticket/api/ticketApi.ts:83-90`
```typescript
const response = await fetch(fullUrl, requestOptions);
const result = await response.json();
```
**Проблема:** Нет проверки статуса ответа перед вызовом `response.json()`. Если ответ не JSON, будет ошибка.

**Рекомендация:**
```typescript
if (!response.ok) {
  throw new Error(`HTTP error! status: ${response.status}`);
}
const result = await response.json();
```

### 🟢 Информационные замечания

#### 9. **Упрощенная генерация ключа запроса**
**Файл:** `entities/ticket/api/ticketApi.ts:11-15`
```typescript
function getRequestKey(randParam: number, apiKey: string): string {
  return btoa(apiKey + randParam).substring(0, 32);
}
```
**Замечание:** В комментариях указано, что в оригинальном проекте используется MD5. Текущая реализация может не совпадать с ожиданиями сервера.

**Рекомендация:** Реализовать MD5 хеширование для совместимости.

#### 10. **Fallback на заглушку для user_id**
**Файл:** `features/tickets/model/useTickets.ts:19-20`
```typescript
console.warn('acsUserId not available, using fallback');
return '1';
```
**Замечание:** Использование заглушки '1' может привести к неправильной работе, если пользователь не авторизован.

**Рекомендация:** Выбрасывать ошибку вместо использования заглушки.

## Рекомендации по улучшению безопасности

1. **Санитизация HTML:**
   - Установить `DOMPurify`: `npm install dompurify @types/dompurify`
   - Использовать для всех HTML-контента перед вставкой в DOM

2. **Валидация входных данных:**
   - Добавить проверки для всех параметров API функций
   - Использовать библиотеку валидации (например, `zod`)

3. **Обработка ошибок:**
   - Добавить try-catch блоки для всех асинхронных операций
   - Правильная обработка ошибок сети и парсинга JSON

4. **Логирование:**
   - Использовать условное логирование для production
   - Не логировать чувствительные данные (токены, пароли)

5. **Переменные окружения:**
   - Убрать все хардкод значения
   - Использовать только переменные окружения
   - Добавить проверку наличия обязательных переменных при старте

## Приоритет исправлений

1. **Высокий приоритет:**
   - XSS через dangerouslySetInnerHTML
   - Хардкод API ключа
   - Обработка ошибок JSON.parse

2. **Средний приоритет:**
   - Валидация входных данных
   - Обработка ошибок сети
   - Замена alert() на UI компоненты

3. **Низкий приоритет:**
   - Удаление console.log
   - Улучшение генерации ключа запроса
   - Улучшение обработки fallback значений

