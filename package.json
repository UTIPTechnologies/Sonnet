{
  "name": "symbol-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "An application to authenticate and view a list of trading symbols via a WebSocket connection. It features a login page and a symbols display page, with state managed by React Context.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test script specified\" && exit 1"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@types/node": "^20.10.0",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.5.3",
    "vite": "^5.0.8"
  }
}
